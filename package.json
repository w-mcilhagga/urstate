{
  "name": "state",
  "version": "0.1.0",
  "description": "state management",
  "main": "src/undoredo.js",
  "directories": {
    "test": "test"
  },
  "babel": {
    "presets": "env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "latest",
    "just-test-it": "latest",
    "uglify-es": "latest"
  },
  "scripts": {
    "test": "just-test-it",
    "bundle": "browserify src/undoredo.js -t babelify -o dist/ur.js",
    "build": "npm run bundle && npm run minify",
    "minify": "uglifyjs dist/ur.js -o dist/ur.js -mc pure_funcs=Object.defineProperty --source-map includeSources,url=ur.js.map"
  },
  "keywords": [
    "state"
  ],
  "author": "william mcilhagga",
  "license": "ISC"
}
