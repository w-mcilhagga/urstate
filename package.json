{
  "name": "state",
  "version": "0.1.0",
  "description": "state management",
  "main": "src/undoredo.js",
  "directories": {
    "test": "test"
  },
  "babel": {
    "presets": "env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "latest",
    "just-test-it": "latest",
    "uglify-es": "latest"
  },
  "scripts": {
    "test": "just-test-it",
    "bundle": "browserify src/undoredo.js -t babelify -o dist/undoredo.js",
    "minify": "uglifyjs dist/undoredo.js -o dist/undoredo.min.js --compress"
  },
  "keywords": [
    "state"
  ],
  "author": "william mcilhagga",
  "license": "ISC"
}
