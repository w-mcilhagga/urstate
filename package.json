{
  "name": "state",
  "version": "0.1.0",
  "description": "state management",
  "main": "src/undoredo.js",
  "directories": {
    "test": "test"
  },
  "babel": {
    "presets": "env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "latest",
    "just-test-it": "latest",
    "uglify-es": "latest"
  },
  "scripts": {
    "test": "just-test-it",
    "bundle": "browserify src/urstate.js -t babelify -o dist/urstate.js",
    "build": "npm run bundle && npm run minify",
    "minify": "uglifyjs dist/urstate.js -o dist/urstate.js --source-map includeSources,url=urstate.js.map"
  },
  "keywords": [
    "state"
  ],
  "author": "william mcilhagga",
  "license": "ISC"
}
